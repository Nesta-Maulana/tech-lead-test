### Backend Test

1. **Setting Environment:**
   - Pastikan file `.env` pada folder `backend-test-1` sudah diset sesuai dengan environment lokal Anda.

2. **Instalasi Dependencies:**
   - Buka terminal dan arahkan ke folder `backend-test-1`.
   - Jalankan perintah `npm install` untuk menginstal semua dependencies.

3. **Migrate Database:**
   - Setelah dependencies terinstal, jalankan perintah `npx sequelize-cli db:migrate` untuk melakukan migrasi database.

4. **Jalankan Test:**
   - Setelah migrasi selesai, jalankan perintah `npx jest` untuk menjalankan test. Jika test sudah selesai, hentikan terminal.

5. **Jalankan Aplikasi:**
   - Terakhir, jalankan perintah `node app.js` untuk menjalankan aplikasi.

6. **Postman Collection:**
   - Postman collection dapat diakses di (https://documenter.getpostman.com/view/12716985/2sA3JT3JUd).

### To Do App Frontend

1. **Instalasi dan Persiapan Database:**
   - Buka terminal dan arahkan ke folder `todo-app`.
   - Jalankan perintah `npm install -g json-server` untuk menginstal JSON Server secara global.
   - Selanjutnya, jalankan `npm install` untuk menginstal semua dependencies.
   - Jalankan `npm run server` untuk menjalankan server database.

2. **Jalankan Aplikasi:**
   - Di terminal yang berbeda, jalankan perintah `npm start` untuk menjalankan aplikasi frontend.

### To Do App Flutter

1. **Persiapan Flutter Project:**
   - Buka terminal dan arahkan ke folder `todo_app_mobile`.
   - Jalankan perintah `flutter pub get` untuk menginstal semua dependencies.

2. **Jalankan Aplikasi:**
   - Terakhir, jalankan perintah `flutter run` untuk menjalankan aplikasi Flutter.

### Test Lainnya

Untuk test lainnya, Anda dapat menemukannya di root folder.
